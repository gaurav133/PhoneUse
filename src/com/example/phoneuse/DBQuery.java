package com.example.phoneuse;

public class DBQuery {
    
 // Common column names.
    public static final String COLUMN_ID = "_id";
    public static final String COLUMN_APP_NAME = "APP_NAME";
    public static final float COLUMN_TIME_SPENT = 0.0f;
    
    //Helper class for getting SQL queries.
    /*public static final String DATABASE_CREATE_FOREGROUND = "create table " + TABLE_FOREGROUND
            + "(" + COLUMN_ID + " integer primary key autoincrement, " + COLUMN_APP_NAME
            + " text not null" + COLUMN_TIME_SPENT + " float not null);";
    public static final String DATABASE_CREATE_BACKGROUND = "create table " + TABLE_BACKGROUND
            + "(" + COLUMN_ID + " integer primary key autoincrement, " + COLUMN_APP_NAME
            + " text not null" + COLUMN_TIME_SPENT + " float not null);";
*/

    
    public static final String GET_ALL_TIMES = "";
    
}
